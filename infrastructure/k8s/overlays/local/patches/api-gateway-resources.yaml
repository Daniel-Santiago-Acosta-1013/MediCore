apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: medicore-api
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: api-gateway
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
